product:
  name: Exocortex
  description: 'Foundation-shaped memory kernel for the Misty Step agent fleet: one binary'
audience: developer
voice: clear, specific, user-facing
changelog:
  source: auto
artifacts:
  markdown: docs/releases/{version}.md
  json: docs/releases/releases.json
release:
  profile: synthesis-only
model:
  policy: balanced
budget:
  max_input_tokens: 12000
  max_output_tokens: 1200
