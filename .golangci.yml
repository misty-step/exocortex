version: "2"

linters:
  default: none
  enable:
    - cyclop
  settings:
    cyclop:
      max-complexity: 15
  exclusions:
    rules:
      - path: '.+_test\.go'
        linters:
          - cyclop
